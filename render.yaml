services:
  - type: web
    name: chat-app
    env: java
    plan: free
    buildCommand: mvn -DskipTests package
    startCommand: java -Dserver.port=$PORT -jar target/chat-app-0.0.1-SNAPSHOT.jar
    envVars:
      - key: SPRING_JPA_DDL
        value: update
